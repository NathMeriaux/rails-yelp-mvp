<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :content %>
  <br>
  <%= f.text_field :content %>
  <br>
  <%= f.label :rating %><br>
  <%= f.text_field :rating %>
  <%= f.submit "add a review" %>
<% end %>
